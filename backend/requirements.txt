# 后端运行与 RAG/记忆/Quick Parse 所需；复制 .env.example 为 .env 后 pip install -r requirements.txt 即可运行
#
# 以下为系统/外部依赖（非 pip 包），需在本机或运行环境中单独安装：
#   - ffmpeg  用于语音识别（ASR）：浏览器上传 webm 时由 pydub 转成 MP3 再调用 Qwen3-ASR-Flash
#   - ngrok   可选，用于将本机后端暴露到公网（调试、移动端访问、Webhook 等）
#
setuptools>=65.0.0,<70.0.0
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0
openai>=1.55.0
httpx==0.26.0
python-dotenv==1.0.0
sse-starlette==1.8.2
minio==7.2.9
redis>=5.0.0
rq>=1.15.0
# FlagEmbedding  # 可选：pip install FlagEmbedding 启用本地 BGE-M3 神经稀疏向量
asyncpg>=0.29.0
pymilvus>=2.4.0
marshmallow>=3.13,<4.0
aio-pika>=9.4.0
elasticsearch==7.17.9
bcrypt>=4.0.0
PyJWT>=2.8.0
pandas
pdfplumber
python-docx
openpyxl
pydub
